name: Run python tests
on:
    push:
        branches:
            - master
    pull_request:
        branches:
            - master

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Install python3
              uses: actions/setup-python@v1
              with:
                  python-version: 3.8
            - name: Install dependancies
              run: |
                  conda create --name step -c conda-forge --yes --file requirements.txt pytest
            - name: Run tests with pytest
              run: conda activate step && pytest

